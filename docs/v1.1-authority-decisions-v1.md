# CAPITAL STEWARD ENGINE
# V1.1 AUTHORITY DECISIONS — GATE V1.1-A

**Status:** AUTHORITY ACCEPTED · IMPLEMENTATION NOT YET AUTHORIZED  
**Recorded:** 2026-08-23  
**Baseline:** Capital Steward Engine v1.0.0 — FROZEN / VALIDATED  
**This artifact:** v1.1 authority record only. It does **not** amend `methodology-freeze-v1.md`, `policy.yaml`, architecture, DecisionPayload, code, tests, or fixtures.

---

## 1. Authority context

GATE V1.1-A prepared four authority questions (CR-018 / CR-009 / CR-007 / CR-015 addendum).  
The authority has **accepted** the options recorded below.

This document is a **governance record**. It is not:

- a methodology-freeze rewrite
- a policy.yaml rewrite
- an implementation specification ready to merge
- authorization to start named-CR coding

The implementation sequence remains:

```
v1.0.0 frozen baseline
    → this authority record
    → named Change Request (implementation authorization)
    → implementation
    → forensic audit
    → UAT
    → v1.1 release
```

The named-CR authorization gate must **not** be skipped.

---

## 2. v1.0.0 immutability statement

The v1.0.0 baseline is **immutable**.

**Must not be modified as a consequence of this record:**

- `docs/methodology-freeze-v1.md`
- `policy/policy.yaml`
- `docs/architecture-and-ui-guidelines.md`
- DecisionPayload v1 (`app/decision.py` authority; `frontend/src/types.ts` serialization)
- backend / frontend source
- tests
- fixtures
- golden decision IDs and expected results
- golden tax fixtures

Where this record **reconciles wording** (e.g. D-04 → `target_top`), that reconciliation is a **v1.1 methodology direction**. It is applied only when a later named CR is authorized to implement and to update successor artifacts. Until then, v1.0.0 files stay as shipped.

The v1.0.0 tag and the baseline commit remain the frozen reference. This file is additive.

---

## 3. Decision A — CR-018 / D-04 / G2 rebalance base

**ACCEPTED: A1 — G2 rebalance base = `target_top`.**

### Canonical rule (v1.1 direction)

G2 fires iff:

```
alloc_pct > max_single_stock_pct          (D-03; 10)
OR
alloc_pct > 1.5 × target_top              (D-02 band high)
```

Then:

```
decision = TRIM-S
target   = band top
```

### Authority notes

| Item | Record |
|---|---|
| Freeze §2 G2 | **Authoritative** executable rule |
| Freeze §14 D-04 | Wording is **reconciled to `target_top`**. D-04 supplies the multiple **1.5**; §2 names the base |
| Shipped v1.0.0 implementation | Already uses `band[1] * rebalance_trigger_multiple` (`app/gates.py`, `app/scoring.py`) |
| Shipped tests | Already use `1.5 × 3` (micro **top**) |
| UI / `policy.yaml` comment | Still say “mid-point” in v1.0.0 — **stale wording**, not a competing rule. Fix only under a later named CR |
| `GOLDEN-G3-HOLD-LT` | **Unchanged** |
| BCC v1.0.0 behaviour | **Unchanged** (6.55% mid is outside A1 G2; would have been inside an A2 window) |
| Golden fixtures | **None modified** |

### Permanent characterisation-test requirements

These windows are where A1 and the rejected A2 (`1.5 × mid-point`) disagree. They are **regression boundaries**, not new methodology:

| Bucket | Disagreement window (alloc %) | A1 (authorised) | A2 (rejected) |
|---|---|---|---|
| Large | **9.0–10.0** | G2 does **not** fire on the band-leg (D-03 binds only above 10) | would G2 |
| Mid | **5.25–7.5** | G2 does **not** fire | would G2 |
| Small / Micro | **3.0–4.5** | G2 does **not** fire | would G2 |

When CR-018 is later implemented/documented, add characterisation tests for these windows. Do **not** treat them as a reason to change SALASAR / ASHOKA / LT / AGI.

**CR-018 status:** AUTHORIZED METHODOLOGY DIRECTION · implementation **not** started · implementation **not** yet authorized as a named build CR.

---

## 4. Decision B — CR-009 / unknown bucket

**ACCEPTED: B1 — Explicitly freeze today’s small/micro fallback.**

### Canonical behaviour (v1.1 direction)

When bucket classification is unavailable (`classify_bucket` has no market cap / no screener row):

```
bucket = null
    → assumed band = small/micro          [1.0, 3.0]
    → G2 band-leg uses Decision A (1.5 × target_top of that band)
    → D-03 absolute cap still applies
    → position_sizing uses the assumed band
    → position_sizing data quality = proxy     (B5)
```

Unknown bucket is **not** a critical missing category (**B6 = NO**).  
It does **not** by itself cap TRIM/HARVEST or disable G2.

### Additional accepted decisions

| ID | Decision |
|---|---|
| **B5** | `position_sizing` data-quality = **proxy** when bucket is unknown |
| **B6** | Unknown bucket is **not** a critical missing category |
| **B7** | `reason_tree` **must** record the band basis |

### Required conceptual provenance (v1.1 contract direction)

```
bucket_basis = assumed_small_micro
```

G2 evidence must be capable of exposing:

```
band_basis
band
alloc_pct
cap_pct
```

This is a **future DecisionPayload v1.1** direction. It is **not** a change to the frozen v1.0.0 payload in this turn.

### AGI Greenpac — permanent regression anchor

```
AGI Greenpac
    allocation = 2%
    decision   = WATCH
    reason     = INSUFFICIENT coverage     (Freeze §3; coverage < 60%)
```

B1 must **not** change this result. Provenance may later *explain* the assumed band; it must not convert AGI into TRIM, EXIT, or NO-DECISION.

**This record does not change the current v1.0.0 implementation.**  
It authorises B1 as the **v1.1 methodology direction**.

**CR-009 status:** AUTHORIZED METHODOLOGY DIRECTION · implementation **not** started · implementation **not** yet authorized as a named build CR.

---

## 5. Decision C — CR-007 / Opportunity Cost

### Accepted answers

| ID | Decision |
|---|---|
| **C1** | PEG proxy **remains** |
| **C2** | D-14 hurdles are **not** operational scorer inputs at this stage |
| **C3** | D-14 remains **signed / provisional** |
| **C4** | Watchlist is **optional** |
| **C5** | If Watchlist is absent, fallback = **PEG proxy** |
| **C6** | **No** change to the 10% Opportunity Cost methodology slot |
| **C7** | Explicit Opportunity Cost provenance is **authorized** |
| **C8** | New fixtures are **additive only** |

### Therefore

Opportunity Cost remains **one** 0–100 category with **D-09 weight 10%**.  
Eligibility and confidence **structure** (Freeze §3 / §4) are unchanged.

### Authorized provenance sources (v1.1 contract direction)

```
opportunity_cost.source ∈ { peg_proxy, watchlist, missing }
```

**Do not** add `hurdle_d14` as an operational source unless a **later** authority decision explicitly activates D-14.

D-14 values (`hurdle_nifty_pct`, `hurdle_liquid_pct`) remain in signed policy as **provisional, unused** by the scorer until that later decision.

### Permanent goldens (never modify)

```
SALASAR → TRIM-S   (G2)
ASHOKA  → EXIT     (G1)
LT      → HOLD     (G3)
AGI     → WATCH    (INSUFFICIENT)
```

The v1.0 golden trilogy, AGI, and the five tax goldens remain permanent regression anchors.

### Additive fixture family (C8) — after a named CR, not now

| Characterisation | Notes |
|---|---|
| Watchlist absent → PEG fallback | Pins C5 |
| Watchlist present | Only after an optional watchlist source exists |
| PEG missing | Distinct from no-fundamentals if a later CR splits them |
| PEG invalid (`≤ 0`) | v1 already maps this to proxy 50; keep as characterisation |
| Future D-14 implementation | **Only after separate authority** to activate D-14 |

**CR-007 status:** AUTHORIZED METHODOLOGY DIRECTION · implementation **not** started · implementation **not** yet authorized as a named build CR.

---

## 6. Decision D — Backtest Methodology Addendum

**ACCEPTED: OPEN the Backtest Methodology Addendum.**

This authorises **preparation of the methodology addendum only**.

### Not authorized

- implementing backtesting
- creating a backtest UI
- optimizing weights
- optimizing thresholds
- altering gates
- altering golden fixtures
- tuning methodology against historical outcomes

### The addendum must address the 18 Gate V1.1-A questions

1. Successful EXIT  
2. Successful TRIM-S  
3. Successful TRIM-V  
4. Successful HOLD  
5. Successful WATCH  
6. Successful HARVEST  
7. Evaluation horizons  
8. Tax treatment  
9. Market / benchmark controls  
10. Sector / theme controls  
11. Survivorship bias  
12. Hindsight leakage  
13. Historical input snapshots  
14. Policy-at-the-time replay (`policy_version` + `content_hash`)  
15. Sold positions  
16. Corporate actions (if in scope)  
17. Outcome data quality  
18. Mechanics-only mode when historical outcome data is insufficient  

### Permanent rule

```
BACKTEST = EVALUATION FRAMEWORK

NOT:

BACKTEST = METHODOLOGY OPTIMIZATION LOOP
```

Historical results must **never** silently redefine the frozen methodology.  
v1 run history / diff remains audit, not this addendum.

**CR-015 status:** AUTHORIZED TO OPEN BACKTEST METHODOLOGY ADDENDUM · implementation **not** authorized.

---

## 7. Dependency map (post-authority)

```
CR-018  A1 recorded
    └── CR-009  B1 recorded (uses A1 G2 rule)
            └── CR-012 G2 sensitivity   still waits on A1 being
                                        implemented and contract-tested

CR-007  C1–C8 recorded
    └── optional watchlist ingest (only if a later CR adds the file)
    └── OC source enum on DecisionPayload v1.1 (named CR)
    └── CR-008 label must match C1/C2/C3 (PEG proxy; D-14 not live)

CR-015
    └── Backtest Methodology Addendum (authorized to open)
            └── archive / sold / forward-path readiness
                    └── implementation (NOT authorized)
```

**Hard blockers that remain:** named-CR implementation authorization for 018 / 009 / 007; addendum completion for 015 code; A1 contract-tests before CR-012 G2 sliders.

**Not hard-blocked by A/B/C (preparation only):** CR-001, CR-005 dry-run, CR-008 (PEG wording), CR-010, CR-011, CR-013, CR-014, CR-016, CR-017, CR-012 **weight-only**.

---

## 8. Permitted next gates

| Gate | Permitted now? | Scope |
|---|---|---|
| Record this artifact | **This turn** | Done by creating this file |
| Named-CR authorization | Next governance step | One CR at a time, scoped |
| GATE V1.1-B | After named-CR auth | CR-001 runtime schema of **v1** payload; v1.1 fields only when 009/007 CRs are authorized |
| GATE V1.1-C | Prepare | CR-005 real three-export **dry-run**; must not invent methodology |
| GATE V1.1-D | Prepare | CR-008 (PEG-proxy copy only), CR-013, CR-010, CR-011, CR-014, CR-016, CR-017 |
| GATE V1.1-E | Prepare weight-only | CR-012 weights; **not** G2 threshold sliders until A1 is implemented + contract-tested |
| GATE V1.1-F | Addendum only | Write Backtest Methodology Addendum; **no** harness, **no** UI |
| v1.1 implementation | **Not permitted** until a named CR is authorized | — |

---

## 9. Prohibited actions

Until a subsequent named-CR implementation authorization exists:

- Do not modify v1.0.0 methodology, policy, architecture, payload, code, tests, or fixtures  
- Do not “clean up” D-04 / WeightsView / `policy.yaml` comments in the frozen tree  
- Do not implement CR-018 / 009 / 007 / 015  
- Do not add `hurdle_d14` as a live OC source  
- Do not treat backtest results as a reason to change gates, weights, or goldens  
- Do not retune `GOLDEN-G2-TRIM-S-SALASAR`, `GOLDEN-G1-EXIT-ASHOKA`, `GOLDEN-G3-HOLD-LT`, AGI WATCH, or the five tax goldens  
- Do not skip named-CR authorization and jump to coding  
- Do not create an implementation branch for these CRs as part of this recording turn  

---

## 10. Golden-fixture preservation rule

The following are **permanent regression anchors**:

| ID / case | Frozen result |
|---|---|
| `GOLDEN-G2-TRIM-S-SALASAR` | TRIM-S · G2 |
| `GOLDEN-G1-EXIT-ASHOKA` | EXIT · G1 |
| `GOLDEN-G3-HOLD-LT` | HOLD · G3 |
| AGI Greenpac | WATCH · INSUFFICIENT coverage |
| Tax goldens | LTCG · STCG · EXEMPTION · S.74 set-off · CROSS-FY |

If a future CR would change any of these **results**, stop. Open a new authority decision. Either keep the golden and change the CR, or issue a **new fixture ID**. Never silently edit these IDs.

New tests from Decisions A/B/C (disagreement windows; unknown @ 5% / 11%; OC PEG fallback) are **additive**.

---

## 11. Change-control rule

1. v1.0.0 remains the frozen executable baseline.  
2. This file records **direction**, not a live spec patch.  
3. Each implementation increment requires a **named Change Request** that cites the Decision (A/B/C/D) it executes.  
4. Contract changes (`bucket_basis`, `opportunity_cost.source`, G2 evidence fields) require a **DecisionPayload v1.1** increment, schema tests (CR-001), and must not break v1 consumers without a version field.  
5. Policy.yaml and methodology-freeze-v1 are updated only in a later authorized CR that produces a **v1.1 freeze addendum** — not by editing the v1.0.0 freeze in place.  
6. Forensic audit + UAT precede any v1.1 release tag.

---

## 12. Dependency state after authority (register)

| CR | State after GATE V1.1-A |
|---|---|
| **CR-018** | AUTHORIZED METHODOLOGY DIRECTION · implementation not started |
| **CR-009** | AUTHORIZED METHODOLOGY DIRECTION · implementation not started |
| **CR-007** | AUTHORIZED METHODOLOGY DIRECTION · implementation not started |
| **CR-015** | AUTHORIZED TO OPEN BACKTEST METHODOLOGY ADDENDUM · implementation **not** authorized |
| **CR-001** | May be **prepared** as a separate implementation CR |
| **CR-005** | Real three-export dry-run may be **prepared**; must not invent methodology |
| **CR-008** | May proceed only with PEG-proxy wording consistent with C1/C2/C3 (D-14 not live) |
| **CR-010, 011, 013, 014, 016, 017** | May be separately prepared where they do not conflict with A/B/C/D |
| **CR-012** weight-only | May be prepared |
| **CR-012** G2 sensitivity | Dependent on A1 being **implemented and contract-tested** |

---

## FINAL STATE

```
V1.0.0                              = FROZEN
GATE V1.1-A                         = AUTHORITY ACCEPTED
CR IMPLEMENTATION                   = NOT YET AUTHORIZED
BACKTEST IMPLEMENTATION             = NOT AUTHORIZED
BACKTEST METHODOLOGY ADDENDUM       = AUTHORIZED TO OPEN
```

No implementation. No baseline modification. Recording only.
